#include <stdio.h>
int main()
{
    int x, y;
    // assignment
    x = 20;
    // assignment
    y = 30;
    int z = x >= y;
    // can print the Value of Z By Using Format Specifier %d
    printf("The Result is  %d", z);
    return 0;
}